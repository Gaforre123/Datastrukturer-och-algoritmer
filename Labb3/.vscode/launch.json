{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Merge Sort",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Insertion_Sort.exe",
            //"program": "${workspaceFolder}/Merge_Sort.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "gdb",
            "preLaunchTask": "build merge"
        }
    ]
}